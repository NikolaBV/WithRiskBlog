[build]
  command = "npm install && npm run build"
  publish = "dist"

# Handle SPA-style routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (will be overridden by Netlify dashboard)
[build.environment]
  NODE_VERSION = "20"

